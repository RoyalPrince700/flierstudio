# Flier Studio (frontend)

Vite + React design studio. Run from repo root with `npm run dev`, or here with `npm install` then `npm run dev`.

See the root [README](../README.md) for monorepo setup, auth, and API.
